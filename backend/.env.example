# Server Configuration
PORT=3000
NODE_ENV=development

# Database
# ========================================
# 🔐 ENVIRONMENT CONFIGURATION
# ========================================
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control
# ========================================

# Server Configuration
PORT=3000
NODE_ENV=development

# ========================================
# 📦 DATABASE CONFIGURATION
# ========================================
# MongoDB Atlas (Recommended - Free Cloud Database)
# 1. Sign up at https://www.mongodb.com/cloud/atlas
# 2. Create a free cluster
# 3. Create a database user with credentials
# 4. Copy your connection string and update MONGODB_URI below
# Note: @ symbol in password must be encoded as %40
# Example: mongodb+srv://username:password%40encrypted@cluster.mongodb.net/database-name?retryWrites=true&w=majority
MONGODB_URI=your_mongodb_connection_string_here

# ========================================
# 🔑 AUTHENTICATION & SECURITY
# ========================================
# JWT Secret - Generate a secure random string
# Run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_random_jwt_secret_key_here
JWT_EXPIRE=7d

# ========================================
# 🗺️ EXTERNAL SERVICES
# ========================================
# Google Maps API Key (optional - for backend validation)
# Get from: https://console.cloud.google.com
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# ========================================
# 🔌 CORS CONFIGURATION
# ========================================
# Comma-separated list of allowed origins
# Examples: http://localhost:3000,http://localhost:19006,exp://your-tunnel-url
CORS_ORIGINS=http://localhost:3000,http://localhost:19006

# ========================================
# 📍 LOCATION TRACKING SETTINGS
# ========================================
# How often to update location (in milliseconds)
LOCATION_UPDATE_INTERVAL=5000
# Maximum number of locations to store in memory
MAX_LOCATION_HISTORY=100
# Or use MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/public-transport

# JWT Secret (generate a random string)
JWT_SECRET=your_jwt_secret_key_here_change_this_in_production
JWT_EXPIRE=7d

# Google Maps API Key (for backend validation if needed)
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:19006

# Location Update Settings
LOCATION_UPDATE_INTERVAL=5000  # milliseconds (5 seconds)
MAX_LOCATION_HISTORY=100       # number of locations to keep in memory
